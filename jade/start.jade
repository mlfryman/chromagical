//- NOTE: hide/show references are commented out until we integrate those functions
#start(ng-hide='!showStart', style='width:{{width}}px; height:{{height}}px;')
  //- .ng-show(ng-hide='!gameStart')
  .row
    .col
      h1.title Chromagic

  .row
    //- need to center this button!
    .col
      button.button.icon-right.ion-wand.button-balanced.button-block#start-btn(ng-click='gameStart(10)') Work your magic!
      p.lead.center Can you match the colors as they fall down?
      h5.subtitle.slide-in-up.center Flex Your Hue Muscles!
